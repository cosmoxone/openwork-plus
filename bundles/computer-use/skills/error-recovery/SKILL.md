---
name: error-recovery
description: GUI 自动化失败时的恢复策略
---

# GUI 自动化错误恢复

当 gui-operate 工具失败时：

1. 读取错误消息，区分：权限不足 / 坐标无效 / 超时 / 目标窗口不存在。
2. 权限不足 → 给出 OS 级权限开启步骤，暂停自动化。
3. 坐标无效 → 重新 screenshot 并更新坐标。
4. 超时 → 最多重试 2 次，仍失败则汇报给用户并停止。

记录失败步骤摘要，便于用户手动接管。